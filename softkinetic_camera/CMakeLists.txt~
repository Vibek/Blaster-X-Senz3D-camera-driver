cmake_minimum_required(VERSION 2.4.6)
include($ENV{ROS_ROOT}/core/rosbuild/rosbuild.cmake)
#include $(shell rospack find mk)/cmake.mk

#set(ROS_BUILD_TYPE Debug)

rosbuild_init()

#set the default path for built executables to the "bin" directory
set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)
#set the default path for built libraries to the "lib" directory
set(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)



#include_directories(${PCL_INCLUDE_DIRS})

#find_package(DepthSenseSDK)

include_directories(/opt/softkinetic/DepthSenseSDK/include)
include_directories(/usr/include/suitesparse)

link_directories(/opt/softkinetic/DepthSenseSDK/lib)

rosbuild_add_executable(softkinetic_cam_start src/softkinetic_start.cpp)
target_link_libraries(softkinetic_cam_start DepthSense)

# Find PCL
#find_package(PCL REQUIRED)

#link_directories(${PCL_LIBRARY_DIRS})
#add_definitions(${PCL_DEFINITIONS})

